/ General JS Setup
javascript:
  // Namespace
  window.Clarat = {
    // Algolia
    Algolia: {
      appID: "#{Rails.application.secrets.algolia['app_id']}",
      apiKey: "#{Rails.application.secrets.algolia['pub_key']}",
      personalIndexName: "Offer_staging_personal",
      remoteIndexName: "Offer_staging_remote"
    }
  };
