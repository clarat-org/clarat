%li.JS-trigger-marker.Listing-results__listitem{id: "result-offer-{{objectID}}"}
  %h3.Listing-results__offer
    %a{href: '{{@root.offers_path}}/{{slug}}', lang: '{{lang}}'}
      = hb 'name'

  / TODO: Refactor and rename
  %h4.Listing-results__offerinfo
    = hb 'ifDiffering encounter from="personal"' do
      %span.Listing-results__contact-type
        = hb 'i18n encounter', scope: 'js.single_search_result.encounters'

    %span.Listing-results__organization
      %span.Listing-results__emphasis
        / TODO: revert to organization_display_name and show stamp elsewhere!
        = hb 'stamp' # = hb 'organization_display_name'
      = hb 'if location_visible' do # only show address if visible
        = hb 'location_address'
